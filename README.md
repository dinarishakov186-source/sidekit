# SideKit

Файлы программы SideKit. Отсюда установленные копии сами скачивают обновления.

- `server.py` — движок
- `index.html` — интерфейс
- `version.json` — номер текущей версии; программа смотрит именно его

Чтобы выпустить обновление, нужно заменить эти файлы и поднять номер в `version.json`.
